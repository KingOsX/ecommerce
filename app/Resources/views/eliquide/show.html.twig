{% extends '@VapamaxMain/main_n2.html.twig' %}

{% block body %}
    <h1>Eliquide</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eliquide.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eliquide_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eliquide_edit', { 'id': eliquide.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
