#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-symlink/bin/grunt-symlink" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-symlink/bin/grunt-symlink" "$@"
  ret=$?
fi
exit $ret
